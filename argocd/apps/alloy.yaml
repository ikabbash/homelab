apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: alloy
  namespace: argocd
spec:
  project: default
  sources:
    # Reference: https://argo-cd.readthedocs.io/en/latest/user-guide/multiple_sources/#helm-value-files-from-external-git-repository
    - repoURL: "https://github.com/ikabbash/homelab.git"
      targetRevision: HEAD
      path: argocd/k8s/alloy/manifests
    - repoURL: "https://grafana.github.io/helm-charts"
      chart: alloy
      targetRevision: "1.6.2"
      helm:
        releaseName: alloy
        valueFiles:
          - $values/argocd/k8s/alloy/values/values.yaml
    - repoURL: "https://github.com/ikabbash/homelab.git"
      targetRevision: HEAD
      ref: values
  destination:
    server: "https://kubernetes.default.svc"
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true